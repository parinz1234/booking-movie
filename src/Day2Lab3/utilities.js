const sum = (a, b) => a + b
const sub = (a, b) => a - b
const multi = (a, b) => a * b
const div = (a, b) => a / b

// .not
const checkNot = 'orange'
// .toHaveLength
const checkToHaveLength = [1  , 2 ,3]
// .toHaveProperty
const checkToHaveProperty = {
  name: 'Parinya',
  age: 24
}

export {
  sum,
  sub,
  multi,
  div,
  checkNot,
  checkToHaveLength,
  checkToHaveProperty
}